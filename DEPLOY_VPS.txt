# DEPLOY NA VPS - COMANDO ÚNICO

cd /opt/athletia && git pull origin main && cd backend && chmod +x scripts/*.sh && npm install && npm run build && bash scripts/criar-estrutura-upload.sh && bash scripts/renomear-media-producao.sh && pm2 restart backend

# Isso vai:
# 1. Fazer pull das alterações
# 2. Instalar dependências
# 3. Buildar o backend
# 4. Criar estrutura de diretórios upload/
# 5. Renomear arquivos exercicio.* para media.* (CORREÇÃO CRÍTICA!)
# 6. Reiniciar o backend

# Para validar (opcional):
bash scripts/validar-sistema-upload.sh

# ============================================================================
# CORREÇÃO URGENTE - RENOMEAR ARQUIVOS DE MÍDIA
# ============================================================================
# Se você só quer corrigir os nomes dos arquivos SEM fazer deploy completo:

cd /opt/athletia/backend && bash scripts/renomear-media-producao.sh

# Depois de renomear, reinicie o backend:
pm2 restart backend

